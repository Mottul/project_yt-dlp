{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Nur was die App wirklich braucht: Ordner wählen und eine fertige Datei im Dateimanager zeigen.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "opener:allow-reveal-item-in-dir"
  ]
}
