name: Release

# Baut die Installer für Windows und macOS.
#   - Tag v*        -> Installer werden an einen Release-Entwurf gehängt
#   - manueller Lauf -> es wird nur gebaut, die Ergebnisse liegen als Artefakte
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            label: macos-universal
            title: macOS (universal)
            # Ein Paket für Intel UND Apple Silicon. `universal-apple-darwin`
            # ist kein rustup-Ziel: Tauri baut beide echten Ziele und fügt sie
            # zusammen -- deshalb müssen beide installiert sein.
            args: --target universal-apple-darwin
            rust-targets: aarch64-apple-darwin,x86_64-apple-darwin
          - platform: windows-latest
            label: windows-x64
            title: Windows (x64)
            args: ''
            rust-targets: ''

    name: ${{ matrix.title }}
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust-targets }}
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
          key: ${{ matrix.label }}

      - uses: tauri-apps/tauri-action@v0
        id: tauri
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: ${{ matrix.args }}
          # Leerer Tag = nur bauen, nichts veröffentlichen (manueller Lauf).
          tagName: ${{ github.ref_type == 'tag' && github.ref_name || '' }}
          releaseName: MottulVideoLoader ${{ github.ref_name }}
          releaseBody: |
            Installation und erster Start: siehe `docs/INSTALL.md`.

            Die Pakete sind nicht signiert — Windows und macOS zeigen deshalb
            beim ersten Start eine Warnung. Die Anleitung beschreibt, wie man
            sie bestätigt.
          releaseDraft: true
          prerelease: false

      - name: Ergebnisse sichern
        if: github.ref_type != 'tag'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.label }}
          # Windows legt unter target/release/bundle ab, der Universal-Build
          # unter target/universal-apple-darwin/release/bundle.
          path: |
            src-tauri/target/**/bundle/**/*.dmg
            src-tauri/target/**/bundle/**/*.exe
            src-tauri/target/**/bundle/**/*.app.tar.gz
          if-no-files-found: error
          retention-days: 7
